x= 0:0.1:20;
y=myGauss(x,10,3)*1000;
plot(x,y);
hold on;
x= mvnrnd(10,3,100);
hist(x);
hold off;
z = mvnrnd(10,3,1000);
muz = mean(z)
sigz = var(z)
index = randi(1000,1,50);
res = z(index);
mures = mean(res)
sigres = var(res)
